---
name: git_clone_concurrency_by_hosts
pack: st2cd
description: Limits the concurrent executions for the git_clone action by hosts.
enabled: true
resource_ref: st2cd.git_clone
policy_type: action.concurrency.attr
parameters:
    threshold: 1
    attributes:
        - hosts
