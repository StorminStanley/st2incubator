---
name: "chatops.deploy"
runner_type: "action-chain"
description: "ChatOps entry point for hubot.deploy"
enabled: true
entry_point: "workflows/chatops_branch.yaml"
parameters:
  message:
    type: string
    description: "Incoming message from ChatOps"
    required: true
  channel:
    type: string
    description: Channel to return ChatOps events to
    required: true
