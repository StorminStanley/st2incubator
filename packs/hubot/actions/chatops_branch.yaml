---
name: "chatops.branch"
runner_type: "action-chain"
description: "ChatOps entry point for hubot.branch"
enabled: true
entry_point: "workflows/chatops_branch.yaml"
parameters:
  channel:
    type: string
    description: Channel to return ChatOps events to
    required: true
