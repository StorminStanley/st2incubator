---
name: merge
runner_type: run-python
description: Merge in action defaults with input from ChatOps
enabled: true
entry_point: 'shell/merge.py'
parameters:
  defaults:
    type: object
    description: "Dictionary of default values"
    required: true
  overrides:
    type: object
    description: "Dictionary of override values"
    required: true
