---
name: asg.add_node.ac
runner_type: action-chain
description: Add a node to an autoscale group (uses ActionChain)
enabled: true
entry_point: 'workflows/asg_add_node.yaml'
parameters:
  asg:
    type: string
    description: Autoscale group to add node
    required: true
