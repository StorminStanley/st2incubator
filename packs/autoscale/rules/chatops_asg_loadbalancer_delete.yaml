---
  name: 'autoscale.chatops.asg.loadbalancer_delete'
  description: 'Delete AutoScale Group'
  enabled: true
  trigger:
    type: 'slack.message'
  criteria:
    trigger.text:
      type: startswith
      pattern: '!asg loadbalancer delete'
  action:
    ref: autoscale.chatops.asg.loadbalancer_delete
    parameters:
      message: '{{trigger.text}}'
      channel: '{{trigger.channel.room}}'
