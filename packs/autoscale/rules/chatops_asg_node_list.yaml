---
  name: 'autoscale.chatops.asg.node_list'
  description: 'Delete AutoScale Group'
  enabled: true
  trigger:
    type: 'slack.message'
  criteria:
    trigger.text:
      type: eq
      pattern: '!asg node list'
  action:
    ref: autoscale.chatops.asg.node_list
    parameters:
      message: '{{trigger.text}}'
      channel: '{{trigger.channel.room}}'
