---
  name: 'autoscale.chatops.asg.node_delete'
  description: 'Delete AutoScale Group'
  enabled: true
  trigger:
    type: 'slack.message'
  criteria:
    trigger.text:
      type: startswith
      pattern: '!asg node delete'
  action:
    ref: autoscale.chatops.asg.node_delete
    parameters:
      message: '{{trigger.text}}'
      channel: '{{trigger.channel.room}}'
