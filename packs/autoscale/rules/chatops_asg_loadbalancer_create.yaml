---
  name: 'autoscale.chatops.asg.loadbalancer_create'
  description: 'Delete AutoScale Group'
  enabled: true
  trigger:
    type: 'slack.message'
  criteria:
    trigger.text:
      type: startswith
      pattern: '!asg loadbalancer create'
  action:
    ref: autoscale.chatops.asg.loadbalancer_create
    parameters:
      message: '{{trigger.text}}'
      channel: '{{trigger.channel.room}}'
