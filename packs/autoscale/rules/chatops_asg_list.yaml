---
  name: 'autoscale.chatops.asg.list'
  description: 'Delete AutoScale Group'
  enabled: true
  trigger:
    type: 'slack.message'
  criteria:
    trigger.text:
      type: eq
      pattern: '!asg list'
  action:
    ref: autoscale.chatops.asg.list
    parameters:
      message: '{{trigger.text}}'
      channel: '{{trigger.channel.room}}'
