---
name: launch-ec2-instance-and-add-to-cluster
runner_type: run-python
description: Launch EC2 instance and add to cass cluster.
enabled: true
entry_point: launch_ec2_instance_and_add_to_cluster.py
parameters:
  cluster_id:
    type: string
    description: "Name of the cass cluster E.g.: Test-Cluster."
    required: true
  launch_config:
    type: string
    description: "Launch configuration as JSON string."
    required: true
