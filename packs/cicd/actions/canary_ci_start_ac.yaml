---
name: ci_start
runner_type: action-chain
description: Workflow on CI beginning
enabled: true
entry_point: 'workflows/ac/ci_start.yaml'
parameters:
  project:
    type: string
    description: Name of the project to be deployed (must match CI Project Name)
    required: true
    position: 1
  branch:
    type: string
    description: Branch of the Project to build.
    required: true
    position: 2
