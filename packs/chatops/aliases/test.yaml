# TODO: remove when done with the pack
---
name: "test"
action_ref: "core.http"
description: "some"
formats:
    - "some {{ url }}" # `hubot some http://httpbin.org/get` is a nice candidate for testing
ack:
  format: "ok"
result:
  format: "done, here it is: {{ execution.result }}"
